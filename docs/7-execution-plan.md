# TodoList 실행계획

**버전:** 1.0.0  
**작성일:** 2026-04-28  
**작성자:** Execution Planning Specialist  
**참조 문서:** 도메인 정의서 v1.1.0, PRD v1.0.0, 사용자 시나리오 v1.0.0, 프로젝트 구조 설계 원칙 v1.1.0, 기술 아키텍처 다이어그램 v1.0.0, ERD v1.0.0

---

## 1. 실행 순서 요약

1. 기반 작업: `DB-01~04`, `BE-01~03`, `FE-01~02`
2. 인증 흐름: `BE-04~06`, `FE-03~04`
3. 핵심 도메인 API/UI: `BE-07~13`, `FE-05~12`
4. 품질/통합 검증: `DB-09`, `BE-14~15`, `FE-13~15`

---

## 2. 미결정 사항 체크리스트

- [ ] JWT 만료 시간 결정
- [ ] Refresh Token Phase 1 제외 여부 결정
- [ ] 비밀번호 최소 길이/복잡도 정책 결정
- [ ] Todo 삭제 방식 결정: 문서 기준으로는 물리 삭제 권장, soft delete 필요 여부 확인
- [ ] Todo 기본 정렬 기준 결정: `created_at DESC` 권장
- [ ] 기한 초과 판단 기준 결정: PRD 가정대로 서버 UTC 기준 권장
- [ ] 카테고리 20개 제한을 DB 트리거까지 둘지, 서비스 트랜잭션으로 보장할지 결정

---

## 3. Database Tasks

### DB-01. PostgreSQL 연결/환경 구성

완료 조건:
- [ ] `DATABASE_URL` 기반 연결이 가능하다.
- [ ] 개발/운영 DB 환경 변수가 분리된다.
- [ ] 연결 실패가 명확한 오류로 전파된다.

의존성:
- [ ] 없음

### DB-02. 마이그레이션 구조 수립

완료 조건:
- [ ] `schema.sql` 또는 `migrations/001-...sql` 구조가 준비된다.
- [ ] 마이그레이션 실행/재실행 방식이 정리된다.
- [ ] 롤백 필요 여부가 결정된다.

의존성:
- [ ] DB-01 완료

### DB-03. 핵심 테이블 생성

완료 조건:
- [ ] `users(id, email, password_hash, created_at)` 테이블이 생성된다.
- [ ] `categories(id, user_id, name, created_at)` 테이블이 생성된다.
- [ ] `todos(id, user_id, category_id, title, description, status, due_date, created_at, completed_at)` 테이블이 생성된다.
- [ ] 평문 비밀번호 컬럼이 없다.

의존성:
- [ ] DB-02 완료

### DB-04. 제약조건/FK/삭제 정책 적용

완료 조건:
- [ ] `users.email` unique 제약이 적용된다.
- [ ] `categories(user_id, name)` unique 제약이 적용된다.
- [ ] `todos.status IN ('in_progress', 'done')` 제약이 적용된다.
- [ ] `title <= 100`, `category.name <= 20` 제약이 적용된다.
- [ ] 사용자 삭제 시 Category/Todo가 cascade 삭제된다.
- [ ] Category 삭제 시 Todo `category_id`가 `SET NULL` 처리된다.

의존성:
- [ ] DB-03 완료

### DB-05. 카테고리 20개 제한 보장

완료 조건:
- [ ] 제한 보장 위치가 결정된다.
- [ ] 동시 생성 요청에서도 21번째 생성이 실패한다.
- [ ] 실패 시 API에서 명확한 오류로 변환 가능하다.

의존성:
- [ ] DB-04 완료

### DB-06. 조회 성능 인덱스 설계

완료 조건:
- [ ] `todos(user_id, created_at)` 인덱스가 검토/적용된다.
- [ ] `todos(user_id, category_id)` 인덱스가 검토/적용된다.
- [ ] `todos(user_id, status)` 인덱스가 검토/적용된다.
- [ ] `todos(user_id, status, due_date)` 또는 partial index가 검토/적용된다.
- [ ] `categories(user_id, created_at)` 인덱스가 검토/적용된다.

의존성:
- [ ] DB-04 완료

### DB-07. Repository 쿼리 규칙 확정

완료 조건:
- [ ] 모든 SQL이 parameterized query로 작성된다.
- [ ] Todo/Category 조회/수정/삭제 쿼리에 `user_id` 조건이 포함된다.
- [ ] `categoryId`, `status`, `overdue` 필터 조합이 안전하게 처리된다.

의존성:
- [ ] DB-06 완료

### DB-08. 상태/완료일시 정합성 쿼리 준비

완료 조건:
- [ ] `done` 전환 시 `completed_at = now()` 처리가 가능하다.
- [ ] `in_progress` 전환 시 `completed_at = NULL` 처리가 가능하다.
- [ ] 완료 Todo는 overdue 조회에서 제외된다.

의존성:
- [ ] DB-07 완료

### DB-09. DB 검증 테스트

완료 조건:
- [ ] 빈 DB에서 마이그레이션이 성공한다.
- [ ] 중복 이메일 저장이 실패한다.
- [ ] 동일 사용자 카테고리명 중복 저장이 실패한다.
- [ ] 다른 사용자 간 동일 카테고리명은 허용된다.
- [ ] 카테고리 삭제 시 Todo `category_id`가 NULL로 변경된다.
- [ ] status/title/category 제약 위반이 실패한다.

의존성:
- [ ] DB-03 완료
- [ ] DB-04 완료

---

## 4. Backend Tasks

### BE-01. 백엔드 앱 기반 구성

완료 조건:
- [ ] `app.js`와 `server.js` 책임이 분리된다.
- [ ] Express 5, CORS, JSON parser, 라우터, 에러 미들웨어가 연결된다.
- [ ] `.env.example`에 필수 환경 변수가 정의된다.
- [ ] 필수 환경 변수 누락 시 서버가 시작되지 않는다.

의존성:
- [ ] 없음

### BE-02. 공통 오류/응답/상수 구성

완료 조건:
- [ ] `{ success: false, message, code }` 오류 응답이 통일된다.
- [ ] `ValidationError`, `UnauthorizedError`, `ForbiddenError`, `NotFoundError`, `ConflictError`가 정의된다.
- [ ] 검증 상수와 JWT 알고리즘 상수가 분리된다.
- [ ] Router -> Controller -> Service -> Repository 구조를 준수한다.

의존성:
- [ ] BE-01 완료

### BE-03. DB 연결 모듈 구성

완료 조건:
- [ ] `pg.Pool` 연결 모듈이 준비된다.
- [ ] 환경 변수만 사용한다.
- [ ] Repository에서 공통 pool을 사용한다.

의존성:
- [ ] BE-01 완료
- [ ] DB-01 완료

### BE-04. 회원가입 API

완료 조건:
- [ ] `POST /api/auth/signup`이 구현된다.
- [ ] 이메일/비밀번호 필수 및 형식 검증이 수행된다.
- [ ] 중복 이메일 오류가 반환된다.
- [ ] 비밀번호가 bcrypt 해시로 저장된다.
- [ ] 응답에 비밀번호 해시가 포함되지 않는다.

의존성:
- [ ] BE-02 완료
- [ ] BE-03 완료
- [ ] DB-04 완료

### BE-05. 로그인/JWT 발급

완료 조건:
- [ ] `POST /api/auth/login`이 구현된다.
- [ ] bcrypt 비밀번호 검증이 수행된다.
- [ ] HS-512 JWT가 발급된다.
- [ ] JWT payload에 사용자 식별자가 포함된다.
- [ ] 인증 실패 시 401이 반환된다.

의존성:
- [ ] BE-04 완료
- [ ] JWT 만료 정책 결정

### BE-06. 인증 미들웨어

완료 조건:
- [ ] Bearer token이 검증된다.
- [ ] 토큰 누락/위조/만료 시 401이 반환된다.
- [ ] 검증 성공 시 `req.user.id`가 설정된다.
- [ ] Todo/Category/Profile API가 보호된다.

의존성:
- [ ] BE-05 완료

### BE-07. 사용자 정보 조회 API

완료 조건:
- [ ] 인증 사용자의 email, created_at을 조회할 수 있다.
- [ ] 민감 정보가 응답에서 제외된다.
- [ ] 미인증 요청은 401을 반환한다.

의존성:
- [ ] BE-06 완료

### BE-08. Category CRUD API

완료 조건:
- [ ] 본인 Category 목록을 조회할 수 있다.
- [ ] Category 생성/수정/삭제가 가능하다.
- [ ] 이름 필수/20자 제한이 적용된다.
- [ ] 동일 사용자 중복 이름이 거부된다.
- [ ] 사용자당 20개 제한이 적용된다.
- [ ] 타 사용자 Category 접근이 차단된다.

의존성:
- [ ] BE-06 완료
- [ ] DB-04 완료
- [ ] DB-05 완료

### BE-09. Category 삭제 후 Todo 미분류 처리

완료 조건:
- [ ] Category 삭제 시 Todo는 삭제되지 않는다.
- [ ] 연결 Todo의 `category_id`가 NULL 처리된다.
- [ ] 삭제된 Category가 조회/필터에 노출되지 않는다.

의존성:
- [ ] BE-08 완료
- [ ] DB `ON DELETE SET NULL` 적용

### BE-10. Todo 생성/조회 API

완료 조건:
- [ ] `POST /api/todos`가 구현된다.
- [ ] 제목 필수/100자 제한이 적용된다.
- [ ] 설명, 종료일, 카테고리 선택값이 처리된다.
- [ ] Category 소유권이 검증된다.
- [ ] 생성 시 `in_progress`, `completed_at = NULL`이 설정된다.
- [ ] `GET /api/todos`는 본인 Todo만 반환한다.

의존성:
- [ ] BE-06 완료
- [ ] BE-08 완료

### BE-11. Todo 수정/삭제 API

완료 조건:
- [ ] 제목/설명/종료일/카테고리를 수정할 수 있다.
- [ ] Todo 소유권이 검증된다.
- [ ] 타 사용자 Todo 접근이 차단된다.
- [ ] Todo 삭제가 구현된다.
- [ ] 생성과 동일한 검증 규칙이 적용된다.

의존성:
- [ ] BE-10 완료
- [ ] Todo 삭제 방식 결정

### BE-12. Todo 상태 전환 API

완료 조건:
- [ ] `in_progress`와 `done` 간 상태 전환이 가능하다.
- [ ] 완료 시 `completed_at`이 기록된다.
- [ ] 진행 중 복귀 시 `completed_at`이 초기화된다.
- [ ] 상태 변경 대상 소유권이 검증된다.
- [ ] 완료 Todo는 overdue에서 제외된다.

의존성:
- [ ] BE-10 완료
- [ ] DB-08 완료

### BE-13. Todo 필터/기한 초과 조회

완료 조건:
- [ ] category 필터가 지원된다.
- [ ] status 필터가 지원된다.
- [ ] overdue 필터가 지원된다.
- [ ] overdue 조건은 `due_date < 서버 기준 오늘` 및 `status = in_progress`이다.
- [ ] 모든 결과는 본인 데이터만 포함한다.
- [ ] 기본 정렬이 일관되게 적용된다.

의존성:
- [ ] BE-10 완료
- [ ] 기본 정렬/타임존 정책 결정

### BE-14. 보안/검증 보강

완료 조건:
- [ ] 모든 SQL이 parameterized query를 사용한다.
- [ ] 서버 측 소유권 검증 누락이 없다.
- [ ] 비밀값 하드코딩이 없다.
- [ ] 400/401/403/404/409 오류 정책이 일관된다.
- [ ] HTTPS는 배포 체크리스트에 포함된다.

의존성:
- [ ] BE-04~BE-13 완료

### BE-15. 백엔드 테스트

완료 조건:
- [ ] 회원가입/중복 이메일/로그인 테스트가 있다.
- [ ] 미인증 접근 401 테스트가 있다.
- [ ] 타 사용자 리소스 접근 차단 테스트가 있다.
- [ ] Todo 제목 100자 초과 테스트가 있다.
- [ ] Category 20개 제한 테스트가 있다.
- [ ] Category 삭제 시 Todo 미분류 테스트가 있다.
- [ ] 완료 Todo overdue 제외 테스트가 있다.

의존성:
- [ ] BE-04~BE-13 완료

---

## 5. Frontend Tasks

### FE-01. 프론트엔드 기반 구조

완료 조건:
- [ ] React 19 + JavaScript + Tailwind 앱이 동작한다.
- [ ] `features/auth`, `features/todo`, `features/category`, `pages`, `lib`, `constants` 구조가 준비된다.
- [ ] TanStack Query Provider가 구성된다.
- [ ] Zustand 기반 UI 상태가 준비된다.
- [ ] `VITE_API_BASE_URL` 기반 API client가 구성된다.
- [ ] JWT 자동 첨부 및 401 처리가 동작한다.

의존성:
- [ ] 없음

### FE-02. 라우팅/인증 보호

완료 조건:
- [ ] `/login`, `/signup`, `/todos`, `/profile` 라우트가 연결된다.
- [ ] 비인증 보호 화면 접근 시 로그인으로 이동한다.
- [ ] 로그인 상태 기본 진입은 Todo 목록이다.
- [ ] 로그아웃 시 토큰 제거 및 로그인 이동이 동작한다.

의존성:
- [ ] FE-01 완료

### FE-03. 회원가입 화면

완료 조건:
- [ ] 이메일/비밀번호 폼이 제공된다.
- [ ] 이메일 형식/필수 검증이 동작한다.
- [ ] 비밀번호가 마스킹된다.
- [ ] 성공 시 로그인으로 이동한다.
- [ ] 중복 이메일 오류가 표시된다.
- [ ] 320px 이상 반응형이 정상 동작한다.

의존성:
- [ ] FE-02 완료
- [ ] BE-04 완료

### FE-04. 로그인 화면

완료 조건:
- [ ] 이메일/비밀번호 폼이 제공된다.
- [ ] 로그인 성공 시 JWT가 저장된다.
- [ ] Todo 목록으로 이동한다.
- [ ] 인증 실패 메시지가 표시된다.
- [ ] 회원가입 이동 링크가 제공된다.

의존성:
- [ ] FE-02 완료
- [ ] BE-05 완료

### FE-05. Todo 목록 화면

완료 조건:
- [ ] Todo 목록을 조회/표시한다.
- [ ] 제목, 설명 요약, 상태, 종료일, 카테고리, overdue가 표시된다.
- [ ] 빈 목록/로딩/오류 상태가 제공된다.
- [ ] Todo 추가, Category 관리, Profile/Logout 동선이 제공된다.
- [ ] 서버 상태는 TanStack Query를 사용한다.

의존성:
- [ ] FE-02 완료
- [ ] BE-10 완료
- [ ] BE-08 완료

### FE-06. Todo 생성 폼

완료 조건:
- [ ] 제목/설명/종료일/카테고리 입력이 제공된다.
- [ ] 제목 필수/100자 제한이 적용된다.
- [ ] 글자 수와 인라인 오류가 표시된다.
- [ ] 카테고리 없이 생성할 수 있다.
- [ ] 성공 시 목록이 갱신된다.
- [ ] 생성 중/실패 상태가 표시된다.

의존성:
- [ ] FE-05 완료
- [ ] BE-10 완료

### FE-07. Todo 상세/수정/삭제

완료 조건:
- [ ] Todo 상세를 확인할 수 있다.
- [ ] 제목/설명/종료일/카테고리를 수정할 수 있다.
- [ ] 제목 검증이 동작한다.
- [ ] 삭제 확인 UI가 제공된다.
- [ ] 성공 시 목록이 갱신된다.
- [ ] 403/404 오류가 표시된다.

의존성:
- [ ] FE-05 완료
- [ ] BE-11 완료

### FE-08. Todo 상태 변경/overdue 표시

완료 조건:
- [ ] 진행 중에서 완료로 변경할 수 있다.
- [ ] 완료에서 진행 중으로 복귀할 수 있다.
- [ ] 완료일시가 표시된다.
- [ ] 진행 중이고 지난 종료일을 가진 Todo가 overdue로 표시된다.
- [ ] 완료 Todo는 overdue에서 제외된다.
- [ ] 상태 변경 중 피드백이 제공된다.

의존성:
- [ ] FE-05 완료
- [ ] BE-12 완료

### FE-09. Todo 필터 UI

완료 조건:
- [ ] Category 필터가 제공된다.
- [ ] 상태 필터가 제공된다: 전체/진행 중/완료.
- [ ] overdue 필터가 제공된다.
- [ ] 필터 변경 시 목록이 갱신된다.
- [ ] 필터 상태는 Zustand로 관리된다.
- [ ] 필터 초기화가 제공된다.

의존성:
- [ ] FE-05 완료
- [ ] BE-13 완료

### FE-10. Category 관리 화면/모달

완료 조건:
- [ ] Category 목록을 조회할 수 있다.
- [ ] Category를 생성할 수 있다.
- [ ] 이름 필수/20자 제한이 적용된다.
- [ ] 중복 이름 오류가 표시된다.
- [ ] 20개 도달 시 추가 제한 안내가 표시된다.
- [ ] 수정/삭제가 제공된다.
- [ ] 삭제 후 필터/선택 목록이 갱신된다.

의존성:
- [ ] FE-05 완료
- [ ] BE-08 완료

### FE-11. Category 삭제 후 Todo 미분류 반영

완료 조건:
- [ ] 삭제된 Category와 연결된 Todo가 `미분류`로 표시된다.
- [ ] 상세/수정 화면에서도 삭제 Category가 선택 상태로 남지 않는다.
- [ ] Category 필터 목록에서 제거된다.
- [ ] 삭제 확인 UI에 미분류 처리 안내가 있다.

의존성:
- [ ] FE-07 완료
- [ ] FE-10 완료
- [ ] BE-09 완료

### FE-12. 사용자 정보 화면

완료 조건:
- [ ] 이메일/가입일이 표시된다.
- [ ] 로그아웃 버튼이 제공된다.
- [ ] 조회 실패 오류/재시도가 제공된다.

의존성:
- [ ] FE-04 완료
- [ ] BE-07 완료

### FE-13. 공통 UX/오류 처리 정리

완료 조건:
- [ ] 모든 API 요청에 로딩 상태가 표시된다.
- [ ] 폼 오류가 인라인으로 표시된다.
- [ ] 서버 오류가 맥락별로 표시된다.
- [ ] 401은 일관되게 로그인으로 연결된다.
- [ ] 생성/수정/삭제 성공/실패 피드백이 제공된다.

의존성:
- [ ] FE-03 완료
- [ ] FE-04 완료
- [ ] FE-06 완료
- [ ] FE-07 완료
- [ ] FE-10 완료

### FE-14. 반응형/접근성 점검

완료 조건:
- [ ] 모바일 320px 이상에서 정상 표시된다.
- [ ] 데스크톱 1280px 이상에서 정상 표시된다.
- [ ] 버튼/입력/필터/모달 터치 영역이 충분하다.
- [ ] 키보드로 폼과 모달을 사용할 수 있다.
- [ ] 긴 Todo 제목/Category명이 레이아웃을 깨지 않는다.

의존성:
- [ ] FE-03~FE-10 완료

### FE-15. 프론트엔드 통합 검증

완료 조건:
- [ ] 회원가입 -> 로그인 -> Category 생성 -> Todo 생성 흐름이 검증된다.
- [ ] 필터링 -> 완료 처리 -> 수정 -> 삭제 흐름이 검증된다.
- [ ] 미인증 접근 시 로그인 이동이 검증된다.
- [ ] Todo 제목 100자 초과가 검증된다.
- [ ] Category 이름 20자 초과/20개 제한이 검증된다.
- [ ] Category 삭제 후 Todo 미분류 표시가 검증된다.
- [ ] Phase 1 제외 범위가 구현되지 않았는지 확인된다.

의존성:
- [ ] FE-01~FE-14 완료
- [ ] BE-04~BE-13 완료

---

## 6. 병렬화 권장 Sprint

### Sprint A. 기반 작업 병렬 진행

- [ ] Database: `DB-01~04`
- [ ] Backend: `BE-01~03`
- [ ] Frontend: `FE-01~02`

완료 조건:
- [ ] DB 스키마 기반이 준비된다.
- [ ] 백엔드 서버 기반이 준비된다.
- [ ] 프론트엔드 앱 기반과 라우팅이 준비된다.

### Sprint B. 인증 흐름 병렬 진행

- [ ] Backend: `BE-04~06`
- [ ] Frontend: `FE-03~04`

완료 조건:
- [ ] 회원가입, 로그인, JWT 인증 흐름이 API와 UI에서 연결된다.
- [ ] 미인증 접근 차단이 동작한다.

### Sprint C. 핵심 도메인 기능 병렬 진행

- [ ] Backend: `BE-07~13`
- [ ] Frontend: `FE-05~12`
- [ ] Database: `DB-05~08`

완료 조건:
- [ ] Todo CRUD, 상태 전환, 필터링이 동작한다.
- [ ] Category CRUD와 삭제 후 미분류 처리가 동작한다.
- [ ] 사용자 정보 조회가 동작한다.

### Sprint D. 품질/통합 검증

- [ ] Database: `DB-09`
- [ ] Backend: `BE-14~15`
- [ ] Frontend: `FE-13~15`

완료 조건:
- [ ] 주요 시나리오가 E2E 수준으로 검증된다.
- [ ] 보안, 검증, 반응형, 접근성 기준이 확인된다.
- [ ] Phase 1 제외 범위가 구현되지 않았음을 확인한다.
